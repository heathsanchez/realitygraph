name: equational-affine-constructor-bank

on:
  push:
    branches: [equational-residual-v1]
    paths:
      - equational_affine_constructor_bank.py
      - equational_residual_demo.py
      - equational_transition_demo.py
      - .github/workflows/equational-affine-constructor-bank.yml
  workflow_dispatch:

concurrency:
  group: equational-affine-constructor-bank-v1
  cancel-in-progress: true

jobs:
  replay:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Replay compiled affine countermodel family
        run: python equational_affine_constructor_bank.py
      - uses: actions/upload-artifact@v4
        with:
          name: equational-affine-constructor-bank-v1
          path: results/equational-affine-constructor-bank-v1.json
          if-no-files-found: error
          retention-days: 3
