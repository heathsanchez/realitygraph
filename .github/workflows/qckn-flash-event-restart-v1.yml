name: QCKN Flash event restart v1

on:
  push:
    branches: [qckn-flash-event-restart-v1]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  red-green:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Run Flash event restart tests
        run: python -m unittest tests.test_flash_event_restart -v
