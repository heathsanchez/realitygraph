name: openml-regression-transfer-v2-manifest
on:
  push:
    branches:
      - openml-regression-transfer-v2
  workflow_dispatch:
concurrency:
  group: openml-regression-transfer-v2-manifest
  cancel-in-progress: true
jobs:
  manifest:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m pip install --quiet openml
      - run: python probe_openml_regression_transfer_v2_manifest.py
      - uses: actions/upload-artifact@v4
        with:
          name: openml-regression-transfer-v2-manifest
          path: openml-regression-transfer-v2-manifest.json
          retention-days: 14
