name: equational-countermodel-premise-transfer

on:
  push:
    branches: [equational-residual-v1]
    paths:
      - equational_countermodel_premise_transfer.py
      - equational_residual_demo.py
      - equational_transition_demo.py
      - equational_internal_lemma_bank.py
      - .github/workflows/equational-countermodel-premise-transfer.yml
  workflow_dispatch:

concurrency:
  group: equational-countermodel-premise-transfer-v1
  cancel-in-progress: true

jobs:
  scan:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Scan sound cross-premise countermodel transfer
        run: python equational_countermodel_premise_transfer.py
      - uses: actions/upload-artifact@v4
        with:
          name: equational-countermodel-premise-transfer-v1
          path: results/equational-countermodel-premise-transfer-v1.json
          if-no-files-found: error
          retention-days: 3
