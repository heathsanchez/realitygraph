name: equational-global-finite-model-bank

on:
  push:
    branches: [equational-residual-v1]
    paths:
      - equational_global_finite_model_bank.py
      - equational_concrete_table_bank.py
      - equational_residual_demo.py
      - equational_transition_demo.py
      - .github/workflows/equational-global-finite-model-bank.yml
  workflow_dispatch:

concurrency:
  group: equational-global-finite-model-bank-v1
  cancel-in-progress: true

jobs:
  replay:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Replay all verified concrete models across all current premises
        run: python equational_global_finite_model_bank.py
      - uses: actions/upload-artifact@v4
        with:
          name: equational-global-finite-model-bank-v1
          path: results/equational-global-finite-model-bank-v1.json
          if-no-files-found: error
          retention-days: 3
