name: equational-countermodel-bank

on:
  push:
    branches: [equational-residual-v1]
    paths:
      - equational_countermodel_bank.py
      - equational_residual_demo.py
      - equational_transition_demo.py
      - .github/workflows/equational-countermodel-bank.yml
  workflow_dispatch:

concurrency:
  group: equational-countermodel-bank-v1
  cancel-in-progress: true

jobs:
  compiled-bank:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Replay verified countermodel bank
        run: python equational_countermodel_bank.py
      - uses: actions/upload-artifact@v4
        with:
          name: equational-countermodel-bank-v1
          path: results/equational-countermodel-bank-v1.json
          if-no-files-found: error
          retention-days: 3
