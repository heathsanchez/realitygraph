name: QCKN Flash external envelope v1

on:
  push:
    branches: [qckn-flash-external-envelope-v1]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  red-green:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Run external event envelope tests
        run: python -m unittest tests.test_flash_external_envelope -v
