name: capability-generator-transfer-v1-manifest
on:
  push:
    branches:
      - capability-generator-transfer-v1
  workflow_dispatch:
concurrency:
  group: capability-generator-transfer-v1-manifest
  cancel-in-progress: true
jobs:
  manifest:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m pip install --quiet openml
      - run: python probe_capability_generator_transfer_v1_manifest.py
      - uses: actions/upload-artifact@v4
        with:
          name: capability-generator-transfer-v1-manifest
          path: capability-generator-transfer-v1-manifest.json
          retention-days: 14
