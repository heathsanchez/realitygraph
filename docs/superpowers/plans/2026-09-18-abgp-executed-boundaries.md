# ABGP Executed Boundaries Implementation Plan

**Goal:** Replace asserted implementation checks with executed boundaries and adversarial evidence; never convert fixtures into scientific confirmation.

**Architecture:** A standalone, declarative finite-policy worker runs after an exec in a clean directory with a kernel syscall allowlist. Acquisition, retained invocation, deletion and reacquisition are separate executions; only allowed JSON fields enter. Runtime construction records stochastic provenance. The existing B control is computed from its actual information instead of a deliberately coarsened signature. G's audit inspects actual finite-support probability mass rather than duplicated synthetic outputs. Qualification reports its actual scope.

**Global constraints:** Keep the normative study files and thresholds unchanged. Do not derive or execute any confirmatory seed. No freeze or collaborator message. The 24-policy family is a DEV mechanism/control test, not a claim of source-distinct scientific persistence. Preserve negative or INVALID scientific outcomes. Do not call merely distinct seed fields complete ancestry. Unsupported Linux isolation fails closed.

## Tasks and acceptance tests

1. `tests/test_abgp_executed_p.py`, `realitygraph/abgp/p_worker.py`, `realitygraph/abgp/executed_p.py`: fresh exec PID and empty environment; invocation payload excludes examples/labels; seccomp blocks file/network/process access; immutable artifact checksum; actual deletion and replay; posterior control optimizes all-four success, not each marginal separately. Run `python -m unittest tests.test_abgp_executed_p -v` RED then GREEN.
2. `tests/test_abgp_ancestry.py`, `realitygraph/abgp/ancestry.py`: typed provenance ledger; shared grandparent hidden below distinct seeds groups units; cross-role reuse is detected; equal independently sampled values do not imply shared ancestry; missing parents or provenance rejected. Run `python -m unittest tests.test_abgp_ancestry -v` RED then GREEN. Wire into executed P with acquisition evidence and future descendants.
3. `tests/test_abgp_honest_controls.py`, `arm_b.py`, `freeze_review.py`, `runner.py`: exact source-history posterior over finite candidate worlds; reveal when ordinary inference matches treatment; G nonexchangeable actual support rejected even with matched schedules, including same-marginals/different-joint examples; missing evidence returns NOT_ESTABLISHED. Remove unsupported grammar/bisimulation qualification flags. Tests fail before code changes and must not demand treatment victories.
4. `tests/test_abgp_qualification.py`, `qualification.py`, `abgp_qualify.py`, workflows: explicitly scope fixture verdict as HARNESS_QUALIFIED; implementation and complete-PASS qualification cannot be inferred from fixtures. Run complete regression and executable-boundary CLI, publish raw worker receipts and checksums, re-read remote commit and CI. No normative edits; document remaining scientific assumptions explicitly in one implementation receipt.
