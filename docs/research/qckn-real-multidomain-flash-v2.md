# QCKN Real Multidomain Flash V2 — Four-Domain Event-Bus Qualification

Authoritative run:
https://github.com/heathsanchez/realitygraph/actions/runs/35409042990

Artifact:
`10572884550`

Artifact digest:
`sha256:ae1eb8e20920f608be1aef2cd539822bbbd28837acf754744c3ddfa084137c38`

## What changed from V1

V1 left Lean-kernel restartable negative reuse as an authority gap.

V2 imports independently qualified Lean restart authority and requires the same
developmental pattern to be supported by four real project domains before it can
materialize cross-domain meta edges:

- SAIR / MathGraph / OpenRouter
- ARC3
- Lean-kernel diagnostic routing
- finite GPU-kernel IR optimization

The shared pattern is:

```text
verified consequence
→ compile durable state
→ restart
→ reuse before reacquisition
→ ablation restores the old work
```

## Four independent authorities

### SAIR

A model-generated, independently verified countermodel bank persisted across
restart and reduced later OpenRouter calls on untouched public SAIR problems.

Typed avoided-cost unit exported to the shared bus:

`sair.openrouter_model_calls = 231`

### ARC3

An exact destination-refuted transfer fingerprint persisted across restart and
eliminated repeated destination-verifier calls while changed scope remained
unblocked.

Typed avoided-cost unit:

`arc.destination_verifier_calls = 8`

### Lean kernel

The qualified restartable negative-routing bank has eight exact source
falsifiers. On the parent-matched public corpus, candidate and parent both retain:

- correct: 182
- UNKNOWN: 7
- wrong: 0
- errors: 0

Routing cost is:

- COLD: 11 verifier calls
- WARM: 3
- RESTART: 3
- SHAM: 11
- ABLATION: 11
- changed residual signature: 11

The strict Arena closure boundary remains explicit: seven UNKNOWN cases remain.

Typed avoided-cost unit:

`lean.diagnostic_verifier_calls = 8`

### GPU finite IR

The prospective finite kernel-IR gate retains exact semantics on 24 run-seeded
targets:

- COLD search: 120
- WARM: 24
- RESTART: 24
- SHAM: 120
- ABLATION: 120

Typed avoided-cost unit:

`gpu_ir.developmental_search_steps = 96`

This remains an IR result, not a CUDA/Triton speed claim.

## The event bus

The global authority-gated event bus begins with **zero cross-domain edges**.

After four-domain independent support is established, V2 emits one exact bridge
certificate per supporting domain into the common developmental domain.

The resulting active edges are exactly:

```text
SAIR        ─┐
ARC3        ─┤
Lean kernel ─┼──> developmental:verified_state_compilation
GPU IR      ─┘
```

There are exactly **4** cross-domain edges.

The bus does not collapse heterogeneous cost units into a fake common scalar.
Attempted scalarization is rejected.

## Content does not leak through the meta bridge

Four real content events are also placed on the bus:

- SAIR finite-magma countermodels
- ARC Action6 transfer content
- Lean diagnostic obstruction content
- GPU-IR optimization transformations

They receive:

**0 cross-domain content edges.**

The existing typed direct-transfer controls also remain:

- direct content attempts: 4
- type mismatches: 4
- repeated identical bad proposals blocked from cache: 4
- repeated destination-verifier calls: 0

Thus the graph distinguishes:

```text
shared developmental operation
!=
shared domain content
```

## Remaining authority residual

Exactly one declared authority residual remains:

`res:gpu-hardware:promote-ir-capability`

The finite GPU-IR developmental result is green, but no CUDA/Triton hardware
latency authority has yet been admitted. Therefore GPU hardware has:

- no meta bridge;
- no hardware speedup claim;
- no inferred authority from the finite IR result.

## Qualification markers

The green run establishes:

```text
PASS_QCKN_REAL_MULTIDOMAIN_FLASH_V2
PASS_FOUR_DOMAIN_REAL_FLASH_EVENT_BUS
PASS_ZERO_LITERAL_CONTENT_BRIDGES
PASS_TYPED_COST_SEPARATION
PASS_GPU_HARDWARE_REMAINS_UNBRIDGED
```

## Interpretation

The strongest supported result is now narrower and stronger than a claim of
universal cross-domain knowledge transfer:

> Four different real developmental domains independently support the same
> higher-order operation: once a consequence is independently established,
> compile the minimum reusable state so later identical work can be avoided.
> The global graph may share that operation through exact authority bridges
> while keeping mathematical, game, kernel and optimization content scoped.

The only missing authority in this first four-domain stack is actual GPU
hardware measurement.
